<% if @feed_items.any? %>
  <ol class="microposts">
    <%= render partial: 'shared/feed_item', collection: @feed_items %>
  </ol>
  <%= page_entries_info @feed_items %>
  <%= will_paginate @feed_items, page_links: page_links %>
<% end %>